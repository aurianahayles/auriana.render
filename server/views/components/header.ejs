<nav class="navbar navbar-expand-lg bg-body-tertiary navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/home"> <i class="fa-brands fa-codepen"></i> WEBD6201</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= (page === 'home') ? 'active' : ''; %>" href="/home">
                        <i class="fa-solid fa-house"></i> Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (page === 'products') ? 'active' : ''; %>"
                       href="/products"><i class="fa-solid fa-heart"></i> Product</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (page === 'services') ? 'active' : ''; %>"
                       href="/services"><i class="fa-solid fa-heart"></i> Service</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (page === 'about') ? 'active' : ''; %>"
                       href="/about"><i class="fa-solid fa-users"></i> About Us</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link <%= (page === 'contact') ? 'active' : ''; %>"
                       href="/contact"><i class="fa-solid fa-earth-americas"></i> Contact Us</a>
                </li>

                <% if (displayName) { %>
                    <li id="contactListLink" class="nav-item">
                        <a class="nav-link  <%= (page === 'contact-list') ? 'active' : ''; %>" href="/contact-list">
                            <i class="fas fa-users"></i> Contact List</a>
                    </li>

                    <li id="contactListLink" class="nav-item">
                        <a class="nav-link  <%= (page === 'update') ? 'active' : ''; %>" href="/update">
                            <i class="fas fa-users"></i> Contact List</a>
                    </li>

                    <li id="logout" class="nav-item">
                        <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                    </li>
                <% } else { %>

                    <li id="login" class="nav-item">
                        <a class="nav-link <%= (page === 'login') ? 'active' : ''; %>"
                           href="/login"><i class="fa-solid fa-sign-in-alt"></i>Login</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
